from stark.apps.world.models import ItemInstance
def parse_command(anima, cmd): pass

"""
    tokens = map(lambda x: x.lower(), cmd.split(' '))

    if tokens[1] == 'wear':
        return wear()
        inventory = anima.inventory()

def wear():
    pass
"""